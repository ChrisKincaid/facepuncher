# Copilot Instructions

## Response behavior

- Execute code edits directly. Do NOT output task lists, plans, or post-edit summaries.
- Run a background syntax/build check after file edits, but DO NOT output text or logs unless a build error is explicitly triggered.
- If the build passes, exit immediately with zero extra text output.
